# API Client SDK

TypeScript SDK for consuming the Platform API. Published to npm.

## Installation
```bash
npm install @buildguard/api-client
```

## Usage
```ts
import { ApiClient } from '@buildguard/api-client';
const client = new ApiClient({ apiKey: 'your-key' });
```
